const emailController = {
  sendEmail: async (req, res) => {
    try {
    //   const { accessToken, deploy, axiosWrapper } = getDataFromReq(req)
    //   const { dealUidsCsv, nominationDate } = req.query
    //   if (!dealUidsCsv || !nominationDate) {
    //     util.response.status(RestStatus.BAD_REQUEST, res, 'Mandatory parameters dealUid, nominationDate')
    //     return;
      }
    //   const dealUids = dealUidsCsv.split(',');

    //   const nominationBatches = await getAllNominationBatches(dealUids, nominationDate, { accessToken, deploy, axiosWrapper })
    //   util.response.status200(res, nominationBatches);
     catch (_err) {
    //   const err = universalError(_err, req.originalUrl);
    //   util.response.status(err.statusCode, res, err.message);
    }
  },
};

module.exports = emailController;
