const path = require('path');

const sender = 'suhane1989@gmail.com';
const subject = 'Red Hot Winter Sale is Onn!!!'
const cwd = path.resolve(process.cwd());

module.exports = {
    sender,
    cwd,
}